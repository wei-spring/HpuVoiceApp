<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="horizontal" >
    
    <LinearLayout
        android:id="@+id/ll_line_menu"
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:layout_height="2px"
        android:background="#88040dff"></LinearLayout>

    <ImageView
        android:id="@+id/iv_menu_item"
        android:layout_below="@id/ll_line_menu"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_launcher"
        />
    <TextView 
        android:id="@+id/menu_item_other_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ll_line_menu"
        android:layout_alignBottom="@id/iv_menu_item"
        android:gravity="center_vertical"
        android:layout_toRightOf="@id/iv_menu_item"
        android:text="菜单"
       />

</RelativeLayout>
